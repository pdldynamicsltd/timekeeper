@using CadentManagement.Authorization
@using CadentManagement.Web.Areas.App.Startup
@{
    ViewBag.CurrentPageName = AppPageNames.Tenant.TimeTrackingMyWeek;
}

@section Styles
{
    <link rel="stylesheet" abp-href="/Common/Styles/dhtmlxscheduler.min.css" asp-append-version="true" />
    <style>
        #myWeekScheduler {
            width: 100%;
            height: 600px;
        }
        .week-summary-bar {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin-bottom: 12px;
        }
        .week-project-badge {
            display: flex;
            align-items: center;
            gap: 6px;
            background: #f5f8fa;
            border-radius: 6px;
            padding: 6px 12px;
            font-size: 13px;
        }
        .week-project-dot {
            width: 10px;
            height: 10px;
            border-radius: 50%;
            flex-shrink: 0;
        }
        .dhx_cal_event .dhx_title {
            font-size: 11px;
        }
        .tt-completed-task .dhx_body,
        .tt-completed-task div {
            opacity: 0.9;
        }
        .tt-completed-task {
            cursor: pointer;
        }
        .tt-completed-task .dhx_body {
            font-weight: 600;
        }
        .tt-completed-task .dhx_title {
            font-size: 10px;
            opacity: 0.85;
        }
        .completed-task-list {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }
        .completed-task-list-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 12px;
            padding: 10px 12px;
            border: 1px solid #e4e6ef;
            border-radius: 8px;
            background: #f8f9fb;
        }
        .completed-task-list-title {
            font-weight: 600;
            color: #181c32;
        }
        .completed-task-list-meta {
            font-size: 12px;
            color: #7e8299;
        }
    </style>
}

@section Scripts
{
    <script src="/Common/Scripts/dhtmlxscheduler.js" asp-append-version="true"></script>
    <script src="/view-resources/Areas/App/Views/TimeTracking/MyWeek.js" asp-append-version="true"></script>
}

<abp-page-subheader title="@L("MyWeek")" description="@L("MyWeekDescription")">
    <button id="LogTimeButton" class="btn btn-primary">
        <i class="fa fa-plus btn-md-icon"></i>
        <span class="d-none d-md-inline-block">@L("LogTime")</span>
    </button>
</abp-page-subheader>

<div class="@(await GetContainerClass())">

    
    <div class="card mb-4">
        <div class="card-body py-3">
            <div class="d-flex align-items-center justify-content-between flex-wrap gap-3">
                <div class="d-flex align-items-center gap-2">
                    <button id="PrevWeekButton" class="btn btn-sm btn-light-primary">
                        <i class="ki-outline ki-arrow-left fs-4"></i>
                        @L("PreviousPeriod")
                    </button>
                    <button id="TodayButton" class="btn btn-sm btn-light">
                        @L("CurrentPeriod")
                    </button>
                    <button id="NextWeekButton" class="btn btn-sm btn-light-primary">
                        @L("NextPeriod")
                        <i class="ki-outline ki-arrow-right fs-4"></i>
                    </button>
                    <span id="WeekLabel" class="fw-bold fs-5 ms-2"></span>
                </div>
                <div class="d-flex align-items-center gap-4">
                    <div class="text-center">
                        <div class="fw-bold fs-3" id="WeekTotalHours">0.0</div>
                        <div class="text-muted fs-7">@L("TotalThisPeriod")</div>
                    </div>
                </div>
            </div>
            <div class="week-summary-bar mt-3" id="WeekProjectSummary"></div>
            <div class="mt-3">
                <div class="fw-semibold fs-6 mb-2">@L("CompletedTasksColumn")</div>
                <div id="CompletedTasksList" class="completed-task-list"></div>
            </div>
        </div>
    </div>

    
    <div class="card">
        <div class="card-body p-0">
            <div id="myWeekScheduler"></div>
        </div>
    </div>

</div>


<div id="timeEntryModalContainer"></div>

